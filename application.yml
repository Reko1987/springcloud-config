spring:
  profiles:
    active:
      - dev


---

spring:
  profiles: dev
  application:
    name: microservice-config-dev
    
    
---
spring:
  profiles: prod
  application:
    name: microservice-config-prod
  
